[pytest]
testpaths = tests
# pythonpath = . lets the tests import the app package (from app.services...)
# when pytest is run from the backend/ directory, without needing an install.
pythonpath = .
addopts = -v
